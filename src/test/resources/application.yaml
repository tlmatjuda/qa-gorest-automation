

# QABASE REST
# ======================================================================================================================
qabase:
  core:
    reports:
      archive:
        enabled: true   # set to false (or omit) to disable
  rest:
    base-url: "https://gorest.co.in/public/v2"